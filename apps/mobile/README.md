# Installation Execution - Mobile App

This directory contains the Flutter mobile app for the installation execution platform.

**Requirements:**
- Flutter SDK (not currently installed in the CI/environment)

Run `flutter create .` to initialize the project once Flutter is available.
