<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 400" width="100%" height="100%">
  <defs>
    <!-- Outer Frame Bevel -->
    <linearGradient id="frameOuter" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#f8f9fa"/>
      <stop offset="100%" stop-color="#e9ecef"/>
    </linearGradient>
    <!-- Frame Inner Bevel -->
    <linearGradient id="frameInner" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#e9ecef"/>
      <stop offset="100%" stop-color="#f8f9fa"/>
    </linearGradient>
    
    <!-- Sash Bevel -->
    <linearGradient id="sashOuter" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="100%" stop-color="#dee2e6"/>
    </linearGradient>

    <!-- Glass Gradient -->
    <linearGradient id="glass" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#c5d3df"/>
      <stop offset="100%" stop-color="#a8bacb"/>
    </linearGradient>

    <!-- Handle Gradient -->
    <linearGradient id="handleMetal" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#fff"/>
      <stop offset="50%" stop-color="#ccc"/>
      <stop offset="100%" stop-color="#999"/>
    </linearGradient>

    <filter id="drop-shadow" x="-10%" y="-10%" width="130%" height="130%">
      <feDropShadow dx="2" dy="4" stdDeviation="4" flood-opacity="0.15"/>
    </filter>
  </defs>

  <!-- Wall/Background shadow -->
  <rect x="20" y="20" width="360" height="360" fill="none" />

  <!-- Outer Window Frame (Outer Edge) -->
  <rect x="25" y="25" width="350" height="350" fill="url(#frameOuter)" stroke="#adb5bd" stroke-width="1.5" filter="url(#drop-shadow)"/>
  
  <!-- Outer Window Frame (Inner Edge) -->
  <rect x="55" y="55" width="290" height="290" fill="#fff" stroke="#adb5bd" stroke-width="1.5"/>

  <!-- Sash (Window Fluegel) Outer Edge -->
  <rect x="58" y="58" width="284" height="284" fill="url(#sashOuter)" stroke="#ced4da" stroke-width="1"/>
  
  <!-- Sash Inner Edge (holding the glass) -->
  <rect x="85" y="85" width="230" height="230" fill="url(#glass)" stroke="#868e96" stroke-width="1.5"/>

  <!-- Glazing Bead Lines (Gasket) -->
  <rect x="87" y="87" width="226" height="226" fill="none" stroke="#343a40" stroke-width="0.5" opacity="0.3"/>
  <rect x="89" y="89" width="222" height="222" fill="none" stroke="#000" stroke-width="1" opacity="0.1"/>

  <!-- Turn-Tilt Lines (Opening Indication) -->
  <g stroke="#6c757d" stroke-width="1" fill="none" opacity="0.8">
    <!-- Turn (Dreh) lines: Hinge is on the right -->
    <line x1="85" y1="85" x2="315" y2="200" />
    <line x1="85" y1="315" x2="315" y2="200" />
    <!-- Tilt (Kipp) lines: Hinge is on the bottom -->
    <line x1="85" y1="315" x2="200" y2="85" />
    <line x1="315" y1="315" x2="200" y2="85" />
  </g>

  <!-- Handle (Left side) -->
  <g transform="translate(68, 180)">
    <!-- Base Plate -->
    <rect x="-4" y="0" width="10" height="40" rx="3" fill="url(#handleMetal)" stroke="#868e96" stroke-width="0.5"/>
    <path d="M-3,5 L-3,35" stroke="#fff" stroke-width="1" opacity="0.5"/>
    <!-- Handle Grip -->
    <path d="M1,20 L1,60 C1,63 -3,63 -3,60 L-3,20 Z" fill="url(#handleMetal)" stroke="#868e96" stroke-width="0.5" filter="url(#drop-shadow)"/>
  </g>

  <!-- Hinge (Top Right) -->
  <rect x="340" y="65" width="6" height="25" rx="1" fill="#fff" stroke="#adb5bd" stroke-width="1"/>
  <!-- Hinge (Bottom Right) -->
  <rect x="340" y="310" width="6" height="35" rx="1" fill="#fff" stroke="#adb5bd" stroke-width="1"/>

  <!-- Panel Number indicator -->
  <text x="200" y="205" font-family="-apple-system, system-ui, sans-serif" font-size="12" font-weight="bold" fill="#343a40" text-anchor="middle">1</text>
  
</svg>
